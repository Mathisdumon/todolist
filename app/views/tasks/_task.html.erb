  <div id="<%= dom_id task %>">

  <p>
      <strong>Name:</strong>
      <%= task.name %>
    </p>

    <p>
      <strong>Description:</strong>
      <%= task.description %>
    </p>

    <p>
      <strong>Duration:</strong>
      <%= task.duration %>
    </p>

    <p>
      <strong>Done:</strong>
      <%= task.done %>
    </p>

  </div>

